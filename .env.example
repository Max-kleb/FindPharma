# Fichier .env.example pour Docker Compose
# INSTRUCTIONS : Copiez ce fichier en .env avant de démarrer l'application
# Commande : cp .env.example .env

# Base de données PostgreSQL
POSTGRES_DB=findpharma_db
POSTGRES_USER=findpharma_user
POSTGRES_PASSWORD=changeme_in_production_12345

# Django Backend
SECRET_KEY=django-insecure-change-this-in-production-xyz123
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend,frontend
DATABASE_URL=postgres://findpharma_user:changeme_in_production_12345@db:5432/findpharma_db

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost

# Email Configuration (Gmail SMTP)
# Compte email du projet : contact.findpharma@gmail.com
# Mot de passe d'application Gmail (ne PAS modifier)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=contact.findpharma@gmail.com
EMAIL_HOST_PASSWORD=cdzrrukhhddbrto
DEFAULT_FROM_EMAIL=contact.findpharma@gmail.com

# React Frontend
REACT_APP_API_URL=http://localhost:8000/api
